"""T4 Research Economics emitter package.

Lives outside `backend/app/` under operator ruling D7 (2026-08-08): T4 creates and
modifies no file inside `backend/app/`. Nothing here is imported by the
application; the direction of dependency is one-way, T4 -> app, never back.
"""
