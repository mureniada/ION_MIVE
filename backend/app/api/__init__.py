"""Transport layer (M8): FastAPI/SSE over the frozen intelligence core.

`service.py` holds framework-agnostic helpers (importable without FastAPI so they
stay unit-testable); `app/main.py` is the thin FastAPI wiring.
"""
