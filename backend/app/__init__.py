"""ION MIVE Lab backend package.

Importing this package (or any submodule) must not require secrets, open network
connections, or construct external clients. All such work happens lazily in
factories at call time (see app/container.py).
"""

__version__ = "0.1.0"
