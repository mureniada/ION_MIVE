GEMINI_API_KEY=
OPENAI_API_KEY=

# Choose and document the vector store configuration used by the new implementation.
VECTOR_STORE_URL=
VECTOR_STORE_API_KEY=
VECTOR_COLLECTION=

# --- Runtime / backend ---
# DEBUG controls the SSE progress stream. true = expose SSE progress (developer mode);
# false = single final REST result only (live/user mode). Never enable DEBUG in a user-facing deployment.
DEBUG=false
API_HOST=0.0.0.0
API_PORT=8000
# Comma-separated origins allowed to call the API (the frontend origin).
CORS_ALLOWED_ORIGINS=http://localhost:5173

# --- Frontend ---
# Base URL the frontend uses to reach the backend REST API.
FRONTEND_API_BASE_URL=http://localhost:8000

# --- Models (confirm exact names + pricing during the research-first phase; do not hardcode blindly) ---
GEMINI_MODEL=
OPENAI_MODEL=
